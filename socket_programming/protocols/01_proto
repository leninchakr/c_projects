CLIENT                                      SERVER
  |                                           |
  | "1-st Message Apple*"                     |
  | "2-nd Message Ball*"                      |
  | "3-rd Message Chocolate*"                 |
  | "4-th Message SkyRoot*"                   |
  |                                           |
  | shutdown(SHUT_WR)                         |
  |------------------------------------------>|
  |                                           |
  |                                  recv() == 0
  |                                  "Peer said SHUT_WR"
  |                                           |
  |                                  traverse MessageList
  |                                           |
  |<--------- message 1 --------------------- |
  |<--------- message 2 --------------------- |
  |<--------- message 3 --------------------- |
  |<--------- message 4 --------------------- |
  |                                           |
  |                                  shutdown(SHUT_WR)
  |<------------------------------------------|
  |                                           |
  | recv() == 0                               |
  | "Peer closed connection..."               |
  |                                           |



Plan:

Server:
        -       Create socket
        -       bind the socket
        -       listen for incomming socket
        -       Wait for client Response
                        -       while()
                                        -       Receive all messages from client
                                        -       Idenfity "*" and print them
                                        -       When peer closes the connection,
                                                        -       Reply back to Client
                                                        -       Close Write-Part

Client:
        -       Create socket
        -       connect with server
        -       Send Multiple messages
        -       Close WR
        -       Now client gets reply from Server
        -       Processes the client
                        -       Identify "!" and print
        -       do Full Closure

